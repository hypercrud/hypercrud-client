(ns hyperfiddle.view.keyboard.combos)

(def alt-backtick #{[:alt "′"] ;; standard
                    [:alt :shift "′"] ;; bépo layout
                    [:alt :grave-accent] ;; US layout
                    })
